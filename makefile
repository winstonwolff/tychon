# Makefile for Tychon


# run web browser

# build Tychon interpretyer (AssemblyScript)

# run tests for interpreter
# watch tests for interpreter

# compile Tychon into JSON

# run parser/compiler tests (Python)
# watch parser/compiler tests

# run Tychon tests
# watch Tychon tests
